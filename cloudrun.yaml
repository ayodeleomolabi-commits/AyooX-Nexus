apiVersion: serving.knative.dev/v1
kind: Service
metadata:
  name: ayoox-nexus
spec:
  template:
    spec:
      containers:
      - image: gcr.io/${GOOGLE_CLOUD_PROJECT}/ayoox-nexus
        resources:
          limits:
            cpu: "1"
            memory: 1Gi
